[project]
name = "video-production-workflow"
version = "0.1.0"
description = "Video production workflow scripts"
requires-python = ">=3.11,<3.14"
dependencies = [
    "funasr",
    "google-genai",
    "imageio",
    "librosa",
    "matplotlib",
    "modelscope",
    "numpy",
    "openai-whisper",
    "pillow",
    "requests",
    "torch==2.7.1+cu126",
    "torchaudio==2.7.1+cu126",
    "python-dotenv",
    "websockets",
]

[project.optional-dependencies]
manim = ["manim"]

[tool.uv]
package = false

[[tool.uv.index]]
name = "pytorch-cu126"
url = "https://download.pytorch.org/whl/cu126"
explicit = true

[tool.uv.sources]
torch = { index = "pytorch-cu126" }
torchaudio = { index = "pytorch-cu126" }
