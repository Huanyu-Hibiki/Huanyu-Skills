---
name: alpha-skill
description: Healthy fixture skill in the fake-opencode tree, used to verify inventory enumeration and cross-agent duplicate detection.
---

# alpha-skill

Static test fixture (no executable logic).

- Valid frontmatter: `name` matches the directory name.
- Short description, well under the 1024-character limit.
- Expected inventory result: healthy (no health issue).

This same skill also exists under `fake-claude/alpha-skill` to provide a
cross-agent duplicate sample.
