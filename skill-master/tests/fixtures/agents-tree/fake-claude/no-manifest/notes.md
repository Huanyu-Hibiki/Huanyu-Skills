# Notes

This directory deliberately contains no `SKILL.md` — only this plain
`notes.md` file.

Expected inventory result: the directory is still enumerated as a skill
entry, with `has_skill_md: false` and health issue `missing_skill_md`.
