---
name: alpha-skill
description: Healthy fixture skill in the fake-claude tree; shares its name with fake-opencode/alpha-skill to provide a cross-agent duplicate sample.
---

# alpha-skill

Static test fixture (no executable logic).

- Valid frontmatter: `name` matches the directory name.
- Short description, well under the 1024-character limit.
- Expected inventory result: healthy on its own, but listed in
  `duplicates` together with `fake-opencode/alpha-skill`.
