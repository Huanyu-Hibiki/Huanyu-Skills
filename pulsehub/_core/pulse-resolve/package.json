{
  "name": "pulse-resolve",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "URL normalizer for Chinese social platforms. Resolves short links, extracts work IDs, preserves tokens.",
  "bin": {
    "pulse-resolve": "./src/cli.ts"
  },
  "scripts": {
    "dev": "tsx src/cli.ts",
    "typecheck": "tsc --noEmit",
    "build": "tsc",
    "test": "vitest run"
  },
  "dependencies": {
    "@pulsehub/types": "workspace:*",
    "axios": "^1.7.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0",
    "vitest": "^2.1.0"
  }
}
