---
name: oracle-cover
description: 从确认标题 + 发布文案生成封面图像 prompt：先确立/复用频道统一视觉框架，再按标题提取本期视觉符号，落盘 prompt/cover/（_base.md 主体 + 各平台比例派生文件）。触发词："封面"/"cover"/"封面提示词"/"生成封面"。
argument-hint: <draft-path>
allowed-tools: Bash(*), Read, Write, Edit, Glob
---

# /oracle-cover — 封面视觉架构师

把标题 + 文案 + 用户档案转换成可直接喂给图像生成工具的结构化 prompt。

## 频道视觉框架（统一记忆点）

**每期封面遵循一个固定的视觉框架**——框架 = 频道的记忆点；场景内容 = 每期根据标题创意生成。

框架来源（按优先级）：
1. **已有 `_base.md` 基准**：作品目录或历史作品已有封面 prompt → 沿用其框架（构图/色调/文字层级/品牌区固定元素）
2. **用户档案定义**：user-profile.md 有形象/视觉规范段 → 按规范生成
3. **首次生成**：与用户协商确立框架（一次定，后续期沿用）——建议框架含：固定构图逻辑（如双场景对照/中心符号/工作台视角）、固定色调体系、固定文字层级（主标题 > 副标题 > 品牌名）、固定品牌区位置

**为什么统一**：观众刷到封面 0.3 秒内靠框架认出"是你"——每期换风格 = 每期从零积累辨识度。

## 输入

| 来源 | 内容 |
|---|---|
| 已确认标题 | title-pick 或用户确认 |
| 发布文案 | oracle-description 产出 |
| `user-profile.md` | 频道名/形象规范/视觉偏好 |
| `<draft-path>`（可选） | 补充内容理解 |

## 目标（必须同时满足）

1. **主标题** = 已确认标题原文（不改写、不翻译、不删减）
2. **副标题** = 从文案提炼一句话价值/悬念，≤18 字（优先 10-16）
3. **画面** = 遵循统一视觉框架 + 本期视觉符号（从标题提取 1 个核心符号：工具/场景/对比物等）
4. **标题排版**：标题 > 16 字 → 给断行版（≤2 行）
5. **负向约束**：无乱码文字、无水印、无签名、无杂乱背景、无低细节、无模糊、不过度暗（人脸/真人按用户偏好）

## 内部工作流

A. 确定视觉框架（见上）
B. 从标题+文案提取本期核心视觉符号
C. 生成副标题（≤18 字）
D. 标题排版（断行判断）
E. 按框架生成场景描述（本期内容决定画什么物件）
F. 生成图像生成 prompt（英文指令 + 中文文字内容引号精确包裹；文字渲染要求放最高优先级：sharp, legible, high-contrast, no gibberish）

## 输出 + 落盘

```
🎨 封面方案（标题：<确认标题>）

内容分析：
  主标题：<标题>（<字数>字 / 排版：<原样|断行版>）
  副标题：<≤18字>
  核心视觉符号：<符号>
  视觉框架：<沿用 EP00N 基准 / 本次新定>

封面构图：
  <中文描述：背景/主标题位置/副标题位置/符号位置/品牌区>

用这个方案落盘？(yes / "调整方向" / "我自己写")
```

用户 yes → **一键落盘**（不只是回消息）：

```
<NNN>_<标题>/prompt/cover/
├── _base.md           # 主体内容（构图/风格/文字/品牌名），跨平台共用
├── prompt-bilibili.md # _base + "Aspect ratio: 16:9"
├── prompt-douyin.md   # _base + "Aspect ratio: 9:16"
├── prompt-xhs.md      # _base + "Aspect ratio: 3:4"
└── prompt-<其他平台>.md # 按 state.platforms 的比例派生
```

流程：
1. 生成 1 份完整主体 prompt（不含比例）→ 写 `_base.md`
2. 各平台拼 `Aspect ratio: <r>` 行 → 写对应 `prompt-<平台>.md`
3. 消息只回 1 次确认 + 文件路径清单（不重复贴 N 份完整 prompt）
4. 用户"调整方向" → 改 `_base.md` → 重拼各平台（不让用户跑多次）

**常见平台比例**：B 站 16:9 / 抖音 9:16 / 小红书 3:4 / 视频号 3:4 / YouTube 16:9。竖版平台注意顶部留白（标题常被 UI 遮挡）。

## 版本保护（不覆盖已采用的 prompt）

为已发布/已被采用封面的作品重新生成 → **不覆盖原文件**，写 `prompt/cover-v2/`（或 -v3）。用户需要对比新旧差异，原 prompt 是回退基准。

**例外**：现有文件是其他环节误写的越界产物且未确认采用 → 直接覆盖为标准结构，不建 v2。

## Scope 边界

- 交付物 = prompt 文件落盘 + 路径清单
- 不生成封面图本身（用户拿 prompt 去图像工具出图）
- 交付后只报清单，不主动催下一步；唯一提醒：predict 锁定前 no-ai-slop 必跑

## Integration

```
oracle-description → oracle-cover（本 skill）→ oracle-no-ai-slop（predict 前必跑）→ 按轨道 review → oracle-predict
```

可选步骤——用户可自己设计。但用了之后封面与标题/文案视觉一致，且框架统一积累辨识度。
